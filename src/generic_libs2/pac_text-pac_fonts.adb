package body pac_text.pac_fonts is

-- 	package body generic_pac_text.generic_child_pac_fonts is

-- 		procedure set_font (
-- 			f : in type_font;
-- 			s : in type_text_size)
-- 		is begin 
-- 			null;
-- 		end set_font;

-- 	end generic_pac_text.generic_child_pac_fonts;

end pac_text.pac_fonts;
